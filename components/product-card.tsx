"use client";
import Image from "next/image";
import { Heart, Plus, Star } from "lucide-react";
import { useLanguage } from "./language-provider";
type Product = { name: string; nameEn: string; price: string; image: string; tag: string; tagEn: string; description: string; descriptionEn: string };
export function ProductCard({ product }: { product: Product }) { const { language }=useLanguage(); const en=language==="en"; const name=en?product.nameEn:product.name; return <article className="group overflow-hidden rounded-3xl bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl hover:shadow-green-900/10"><div className="relative aspect-square overflow-hidden bg-mist/30"><Image className="object-cover transition duration-500 group-hover:scale-105" src={product.image} alt={name} fill sizes="(max-width: 640px) 50vw, 25vw"/><span className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1 text-[10px] font-semibold uppercase tracking-wide text-forest">{en?product.tagEn:product.tag}</span><button className="absolute right-3 top-3 grid h-9 w-9 place-items-center rounded-full bg-white/90" aria-label={`${en?"Add to wishlist":"Favoritkan"} ${name}`}><Heart size={16}/></button></div><div className="p-5"><div className="mb-2 flex items-center justify-between"><h3 className="font-semibold">{name}</h3><span className="flex items-center gap-1 text-xs text-amber-500"><Star size={12} fill="currentColor"/>4.9</span></div><p className="mb-4 text-xs leading-5 text-stone-500">{en?product.descriptionEn:product.description}</p><div className="flex items-center justify-between"><strong className="text-forest">{product.price}</strong><button aria-label={`${en?"Add":"Tambah"} ${name}`} className="grid h-9 w-9 place-items-center rounded-full bg-forest text-white"><Plus size={17}/></button></div></div></article> }
