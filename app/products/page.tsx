"use client";
import { Header } from "@/components/header"; import { Footer } from "@/components/footer"; import { ProductCard } from "@/components/product-card"; import { products } from "@/lib/data"; import { useText } from "@/components/language-provider";
export default function Products() { const t=useText(); return <><Header/><main className="shell py-16"><p className="eyebrow">{t("Dari kebun & untuk kebun","From our farm, for your garden")}</p><h1 className="text-5xl font-semibold tracking-tight">{t("Produk pilihan kami","Our selected products")}</h1><div className="mt-10 flex flex-wrap gap-2">{[["Semua","All"],["Sayur segar","Fresh vegetables"],["Nutrisi","Nutrients"],["Media tanam","Growing media"],["Peralatan","Equipment"]].map(([id,en],i) => <button className={`rounded-full px-4 py-2 text-sm ${i===0?"bg-forest text-white":"bg-white border"}`} key={id}>{t(id,en)}</button>)}</div><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{products.map(p => <ProductCard key={p.name} product={p}/>)}</div></main><Footer/></> }
